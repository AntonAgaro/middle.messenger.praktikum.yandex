export default `

    <form class="form modal">
        <h3>{{serverError}}</h3>
            <h2 class="form__title">{{ title }}</h2>
            {{{ input }}}
            {{{ button }}}
    </form>

`
