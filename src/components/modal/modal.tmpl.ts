export default `

    <div class="modal">
        <h3>{{serverError}}</h3>
        <form class="form">
            <h2 class="form__title">{{ title }}</h2>
            {{{ input }}}
            {{{ button }}}
        </form>
    </div>

`
